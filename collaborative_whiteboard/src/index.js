
import React from 'react';
import ReactDOM from 'react-dom';
import Whiteboard from './Whiteboard';
import './styles.css';

ReactDOM.render(
  <React.StrictMode>
    <Whiteboard />
  </React.StrictMode>,
  document.getElementById('whiteboard-root')
);
